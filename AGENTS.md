# AGENTS.md — 數學冒險專案維護指南

> 本文件是給後續 Codex／AI 開發代理使用的長期專案說明。開始任何工作前，先閱讀本文件，再依任務只開啟相關模組。  
> 本文件以目前 `outputs/` 內的實際程式碼為準；舊對話曾出現但已撤回、未合併或只停留在構想的內容，會明確標示，不可當成既有功能。

## 0. 專案根目錄與版本來源

- 本工作區中可部署的完整專案根目錄是本文件所在的 `outputs/`。
- 主要入口為 `outputs/app.py`，不是工作區外曾使用過的 `D:\math-learning-app\app.py`。
- 本工作區的 `outputs/` 本身沒有可用的 `.git`；公開版過去主要透過將檔案上傳到 GitHub，再由 Streamlit Community Cloud 部署。
- 使用者常同時保有：
  - 本工作區 `outputs/`；
  - 另一份本機測試目錄；
  - GitHub／Streamlit 公開版。
- **每次修改前一定要確認使用者要改哪一份，以及公開站目前實際部署哪個 commit／檔案。** 不可因本機正常就假定公開版已同步。
- 本文件適用於目前的「五章數學版」。先前曾試作「國英數自四科」與《知識勇者：魔王試煉》，但目前程式已回到數學版；除非使用者重新明確啟動多科改版，不要自行恢復該試作。

---

## 1. 遊戲與專案概述

### 1.1 遊戲定位

這是一款面向國小高年級／國中小學生的數學練習 RPG。學生以勇者身分完成限題數的數學連擊挑戰，取得星級、經驗值、裝備與培養資源，再挑戰章節普通 BOSS 與菁英 BOSS。核心目的不是製作純戰鬥遊戲，而是以收集、養成、排行和短動畫提高重複練習意願。

目前正式內容共五章：

1. 整數加減法（3 單元）
2. 整數乘除法（4 單元）
3. 小數加減法（2 單元）
4. 小數乘除法（4 單元）
5. 因倍數與分數（3 單元）

### 1.2 核心遊戲循環

1. 學生以學生代碼與 6 位 PIN 登入。
2. 選擇已解鎖單元，最多作答 20 題，以連擊取得 0～3 星。
3. 單元結束結算最佳星級差額 EXP、裝備、擊殺券與寵物培養道具。
4. 章節單元達成條件後挑戰普通 BOSS；通過普通 BOSS 才解鎖下一章及本章菁英 BOSS。
5. 收集／更換裝備、培養及跟隨寵物、完成任務、查看排行榜。
6. BOSS 首通給一次性 EXP、資源／四星裝備／稱號；重複挑戰只更新最佳時間等可重複內容。

### 1.3 技術棧與執行環境

- 語言：Python 3（開發歷程主要使用 Python 3.13）。
- Web UI：Streamlit，版本限制 `>=1.40,<2`。
- 圖片處理：Pillow `>=10,<13`。
- 本機資料庫：SQLite，預設 `game.db`，啟用 WAL。
- 公開資料庫：PostgreSQL／Neon，使用 `psycopg[binary,pool]>=3.2,<4`。
- 部署：GitHub + Streamlit Community Cloud。
- 時區：遊戲日界線與顯示時間以 `Asia/Taipei` 為準。
- 圖像／動畫：`assets/` 中的 PNG、WebP、MP4；戰鬥畫面另使用 HTML/CSS/JS 動畫。
- 機密設定：`.streamlit/secrets.toml` 或 Streamlit Cloud Secrets 中的 `DATABASE_URL`、`ADMIN_PIN`。機密檔不可提交或輸出內容。

### 1.4 啟動與基本驗證

在專案根目錄執行（Windows 若 `python` 不在 PATH，使用實際 Python 執行檔）：

```powershell
python -m streamlit run app.py
```

最小靜態驗證：

```powershell
python -m compileall -q app.py data_access game_logic game_ui game_data
```

目前盤點時，入口及所有模組已通過 `compileall`。本機與公開版資料來源不同，所以資料相關測試必須說明使用 SQLite 還是 Neon。

---

## 2. 目前專案架構

### 2.1 根目錄

| 路徑 | 職責 | 注意事項 |
|---|---|---|
| `app.py` | Streamlit 入口、session state、頁面路由、跨系統編排、部分尚未抽離的規則與資料遷移 | 約 3,000 行，仍是主要耦合點；不要無目的通讀或大改 |
| `requirements.txt` | 部署依賴 | 更動版本前須在本機與 Cloud 驗證 Streamlit DOM/CSS |
| `.gitignore` | 排除本機 DB、Secrets、快取及 archive | `game.db`、Secrets 絕不可上傳 |
| `.streamlit/` | 本機 Streamlit 設定／Secrets | 不得揭露 `secrets.toml` |
| `game.db` | 本機 SQLite 測試資料 | 與公開 Neon 完全不同；已被忽略 |
| `assets/` | BOSS、勇者、登入背景、大頭貼、寵物、技能等媒體 | 部署缺檔會直接造成 Streamlit 媒體錯誤；引用路徑不可隨意搬動 |
| `archive/` | 舊版備份 | 不屬於執行路徑；只供回溯，不應當成現行程式修改 |
| `make_login_animation.py` | 登入動畫素材產生輔助腳本 | 非主程式執行依賴 |

### 2.2 `game_data/`：靜態設定

| 檔案 | 職責 |
|---|---|
| `game_data/config.py` | 章節、單元、BOSS、裝備部位、固定數值、詞條、題數等核心設定 |

主要依賴者：`app.py`、題目／裝備／戰鬥邏輯及多數 UI 模組。修改設定值會跨系統生效；尤其 BOSS key、章節 ID、單元 ID、裝備 slot 名稱屬於穩定識別碼。

### 2.3 `data_access/`：資料庫層

| 檔案 | 職責 |
|---|---|
| `database.py` | SQLite／Postgres 共用連線介面、`?` 到 `%s` 參數轉換、Neon 讀取斷線重試、連線池 |
| `schema.py` | 建表與相容欄位遷移 |
| `players.py` | 玩家建立、驗證、取得與儲存 `profile_json`、老師測試角色 |
| `progress.py` | 排行、作答嘗試、題目紀錄 |
| `admin.py` | 老師端查詢資料 |
| `communications.py` | 信箱、公告、遊戲回饋 |

資料庫目前包含：

- `settings`
- `players`
- 第一至第五章普通／菁英 BOSS 各自的排行表（共 10 表）
- `attempts`
- `question_logs`
- `game_feedback`
- `mailbox`
- `announcements`

`players.profile_json` 是主要遊戲存檔。裝備、物品、兩套配裝、寵物、貨幣、任務、章節旗標等大多沒有正規化成獨立資料表。

### 2.4 `game_logic/`：純規則／領域邏輯

| 檔案 | 職責 | 主要依賴 |
|---|---|---|
| `authentication.py` | PIN 雜湊驗證、名稱規則、登入 token 等 | `app.py`、登入 UI |
| `questions.py` | 16 個單元的題目與答案產生 | `game_data/config.py` |
| `progression.py` | EXP、解鎖、星級、每日／永久／特殊任務等規則 | `config.py`、`app.py` 編排 |
| `equipment.py` | 裝備固定值、顯示文字、角色能力計算 | `config.py`、`pets.py` |
| `loot.py` | 隨機掉落、四星固定獎勵、收藏判定協助 | `config.py`、`equipment.py` |
| `economy.py` | 商店、刷新、購買、分解、熔煉與特殊熔煉石 | `profile.py` |
| `combat.py` | 決定性的 BOSS 戰鬥模擬，只回傳事件資料 | `config.py` |
| `profile.py` | 新存檔預設值、舊存檔正規化、兩套配裝同步、永久收藏目錄 | `pets.py` |
| `pets.py` | 六寵物目錄、擁有／跟隨、排序、每日召喚、重複轉元神、進階、培養與等級加成 | `assets/pets/pets.json` |

原則上 `game_logic/` 不應直接渲染 Streamlit。現況大致遵守此原則；跨系統存檔和通知仍由 `app.py` 負責。

### 2.5 `game_ui/`：介面層

| 檔案 | 職責 |
|---|---|
| `common.py` | 共用 CSS／JS、置頂、返回按鈕等介面輔助 |
| `login.py` | 登入背景、登入／註冊版面、大頭貼編輯 |
| `profile.py` | 能力顯示、物品更換前後比較 |
| `character.py` | 角色能力對話框、兩套裝備、未使用裝備、多項分解、寵物頁、稱號頁 |
| `inventory.py` | 背包消耗道具與圖鑑 |
| `economy.py` | 商店與熔煉工坊 UI |
| `quiz.py` | 作答及結算 UI |
| `stages.py` | 章節／單元／BOSS 卡片 UI |
| `battle.py` | BOSS 準備、血條、勇者／BOSS 圖片、技能與戰鬥動畫 |
| `pet_summon.py` | 寵物召喚主頁、機率、圖鑑、召喚結果 |
| `admin.py` | 老師後台 UI、學生資料與戰鬥模擬等 |

### 2.6 `assets/`：媒體資產

- `assets/bosses/`：五章普通及菁英 BOSS 圖。
- `assets/heroes/`：男女勇者 WebP 及高解析 PNG；角色能力視窗與戰鬥依性別選圖。
- `assets/effects/`：技能特效素材。
- `assets/login/`：直式／橫式登入背景與動畫。
- `assets/avatars/`：20 個內建頭像。
- `assets/pets/`：六隻寵物的 `icon`、`chibi`、`mythic` 圖、召喚集合圖與 `pets.json`。

### 2.7 主要呼叫方向

```text
app.py（路由／session／跨系統交易）
├─ game_ui/*（只負責畫面與互動）
│  └─ game_logic/*（規則、計算、存檔物件操作）
├─ game_logic/*
│  └─ game_data/config.py、assets/pets/pets.json
└─ data_access/*（SQLite／Postgres）
```

重要例外：`app.py` 仍保留大量任務、獎勵、遷移、信箱與頁面狀態編排。未來可以逐步抽離，但不可在單一功能修改時順便全面重構。

---

## 3. 已完成並已進入目前程式的功能

本節表示「程式碼確實存在」。部分手機 UI 仍需回歸測試，不等同所有裝置都完全無 Bug。

### 3.1 帳號與老師後台

- 學生自行建立勇者：正式姓名、公開勇者名稱、順序學生代碼、6 位 PIN。
- 學生代碼／PIN 登入；可選 5 分鐘保持登入。
- 台灣時間 22:00～08:00 禁止學生登入；老師不受限制。
- 公開版若缺 `ADMIN_PIN` 會停止登入以保護後台。
- 名稱格式與重複檢查；PIN 使用 salt + hash，不以明文存放。
- 老師測試角色、帳號建立／刪除／重設 PIN／修改正式姓名。
- 老師可檢視學生進度、能力、裝備、背包、作答紀錄及錯題。
- 老師端戰鬥模擬器可使用公開資料中的玩家存檔。
- 老師可補發全部六隻寵物給指定玩家／測試角色。

### 3.2 單元、星級、經驗與解鎖

- 每回合最多 20 題；以最高連擊判定星級。
- 第一至四章：1～4 連擊一星、5～9 二星、10 以上三星，10 連擊自動結束。
- 第五章起：1～3 一星、4～6 二星、7 以上三星，8 連擊自動結束。
- 沒答對不給星、EXP 與獎勵。
- 單元 EXP 只領最佳星級差額：一／二／三星累計上限為 20／40／60。
- 玩家升級需求為目前等級 ×100 EXP；等級提升時 HP、攻擊、防禦基礎值各增加 Lv1 基礎的 10%。
- 章內單元依序解鎖；下一章需通過前一章普通 BOSS；菁英 BOSS 不阻擋下一章。
- 已通過單元可使用擊殺券快速結算裝備，不重複給 EXP。

### 3.3 題目系統

- 目前 16 個數學單元均有隨機題目產生器。
- 含整數加減乘除、小數精確運算、最大公因數、最小公倍數、最簡分數。
- 小數題已修正過浮點誤差與百分位為 0 等顯示／答案問題。
- 最簡分數以分子／分母分開輸入，且要求最簡形式；等值但未約分答案不算正確。
- Enter 送出與輸入框重新聚焦已有處理；手機數字鍵盤由輸入類型與 UI 控制。
- 作答紀錄會保存題目、學生答案、正解、對錯及時間，老師端可查最近紀錄。

### 3.4 裝備、收藏、背包與配裝

- 9 個裝備部位及第 10 格跟隨寵物顯示。
- 兩套裝備配置，可切換並自訂配置名稱。
- 裝備固定值依章節及星級計算；單元最多掉三星。
- 四星為章節固定獎勵，各章三件，名稱會補上「第 X 章・」前綴。
- 裝備詞條、能力加總、比較目前／更換後能力。
- 未使用裝備已移至角色能力對話框，支援篩選、單件／多項分解。
- 背包目前專門收納消耗道具；圖鑑為獨立功能。
- 永久 `collection_catalog` 記錄曾取得裝備；分解後不會倒退為未收藏。
- 同單元同星級重新刷取可得到不同詞條，並避免完全相同簽名。
- 掉落會優先補尚未收藏的同星級部位。

### 3.5 商店與熔煉

- 商店一次 6 件、三星裝備、每件 500 金幣；內容依最高進度章節產生。
- 自然刷新 24 小時；購買後標示售完。
- 強制刷新成本每按 5 次加倍：100、200、400……；自然刷新會重設付費刷新次數。
- 分解：一星 100 金幣、二星 200、三星 300 + 1 普通熔煉石；四星不可分解。
- 3 件同章節、同星級裝備熔煉：3 一星→1 二星；3 二星 +1 普通熔煉石→1 三星。
- 部位／基礎詞條／進階詞條熔煉石，各以 5 顆普通熔煉石合成；熔煉時可指定對應結果。
- 熔煉結果有獨立對話框，可立即裝備或放回物品欄。

### 3.6 BOSS 與戰鬥

- 五章各有普通與菁英 BOSS，數值及技能由 `BOSS_CONFIGS` 管理。
- 戰鬥模擬是決定性的事件序列；角色數值相同時結果相同。
- 防禦公式：`傷害 × 100 ÷ (100 + 有效防禦) × (1 - 傷害減免)`。
- 暴擊率預設 0；角色暴擊以固定間隔近似，暴擊倍率為 `1.5 + 暴擊傷害加成`。
- 菁英專用詞條只在菁英戰生效。
- 一般／菁英 BOSS 首通 EXP、最佳時間排行、稱號與四星掉落已實作。
- 戰鬥動畫包含男女勇者、BOSS 圖、傷害／爪痕、火龍斬、雷劫、狂風驟雨等演出。
- 歷史最佳時間保留，不因後續 BOSS 平衡調整重算。

### 3.7 任務、排行、通訊

- 每日登入、每日最高進度章節練習兩次、永久章節任務、10 秒內 BOSS 特殊任務。
- 任務可一鍵領取；已完成項目移至下方。
- 排行包括章節普通／菁英 BOSS、等級、HP、攻擊、防禦、攻速；學生看前 10 名並能看到自己的名次。
- 公告可新增、停用、編輯舊公告並重新啟用；內容保留分行並格式化編號。
- 遊戲回饋可分類送出；老師可查看、回覆，已回覆狀態會記錄。
- 信箱支援未讀／已讀、單封與一鍵領收、獎勵 JSON。

### 3.8 寵物、召喚與培養（已實作部分）

- 六屬性與六隻寵物資料、神話／Q 版／小圖資產均存在：
  - 木：綠帽鼠
  - 土：巧克力蛇
  - 水：果凍魚
  - 火：赤尾狗
  - 暗：心機狐
  - 光：大橘
- 新舊玩家都靜默擁有一星大橘；老師可授予所有寵物。
- 寵物頁可排序、切換神話／Q 版圖、左右瀏覽、跟隨、改名、培養與進階。
- 跟隨寵物會進入第十格，並在角色能力顯示攻擊屬性及顏色。
- 寵物 Lv5／10／15／20 分別提供 HP +25%、攻擊 +25%、防禦 +25%、攻速 +0.25/秒。
- 寵物不得超過玩家等級，最高 Lv20；每級需求等級 ×100 EXP。
- 美味罐頭 +30 EXP；同屬性特製仙丹 +50 EXP，可一次選擇使用數量。
- 每次單元通關掉 2 個美味罐頭；普通／菁英 BOSS 首通分別以信箱給 5／10 個，舊玩家會依通關紀錄補發。
- 召喚每日 08:00 重置：免費 1 次、付費 10 次。
- 付費召喚優先消耗召喚券，無券則消耗 2,000 金幣。
- 普通 BOSS 章節首通補發 2 張召喚券（以信箱處理）。
- 召喚池內候選等機率；重複寵物轉為同屬性元神。
- 一星→二星消耗 2 元神；二星→三星消耗 6 元神。
- 心機狐目前由程式寫死為 **2026-09-15 之後退出召喚池**；既有寵物不受影響。
- 十隻 BOSS 已設定元素：第一章暗／暗、第二章土／暗、第三章木／火、第四章光／光、第五章水／水，章節 BOSS 卡會顯示屬性。
- 六隻寵物的三星技能資料與戰鬥數值已接入；技能僅在三星寵物跟隨時生效，戰鬥事件以 `event_type="pet_skill"` 標記，供後續動畫使用。

---

## 4. 目前正在開發／尚未完成

### 4.1 角色能力對話框是目前主要開發區

- 角色能力使用 `st.dialog` 覆蓋首頁；對話框包含配裝、未使用裝備、寵物、稱號四頁。
- 最近修正：不再用自製 fixed 關閉按鈕，改用 Streamlit 原生 dialog 關閉鈕與 `on_dismiss`，以解決橫／直屏切換後關閉鈕消失及空白頁。
- 這項修正已在本機以重複開關最小測試驗證；**仍應以實際手機、公開部署進行直屏↔橫屏回歸測試**。
- `game_ui/character.py` 約 1,500 行，大量 CSS 針對直屏；這是最脆弱的 UI 模組。
- 最新使用者要求是「最後一次修正關閉鈕偶爾消失／空白」。目前程式層已採原生 dialog，但公開版是否已更新無法從本工作區確認。

### 4.2 寵物戰鬥尚未完整

已存在：

- 跟隨寵物的等級能力加成。
- 攻擊屬性名稱、顏色及 10% 額外裝備掉落率 metadata。
- 六屬性資料與剋制關係資料。

目前狀態：

- `BOSS_CONFIGS` 已有 `element` 欄位，六種三星寵物技能已進入 `combat.simulate_battle()`。
- 寵物頁「技能」可查看完整技能效果；未三星仍可查看，但會提示需進階到三星才能解鎖。
- 一般攻擊已依跟隨寵物屬性套用元素剋制 ±15%；所有星級皆生效，三星技能效果另外疊加。
- 三星技能已有結構化戰鬥事件與文字，但專屬視覺動畫尚未製作。

### 4.3 召喚結果動畫

- 目前有召喚結果畫面與圖片，但對話中規劃的聖光降臨、寵物出現動畫尚未確認為正式動畫流程；目前程式主要是結果提示與圖片。

### 4.4 待辦建議順序

1. 公開版手機回歸測試角色能力對話框：開啟、四頁切換、旋轉、關閉、重開。
2. 為角色能力 UI 建立最小可重現測試頁或視覺檢查清單，減少持續覆寫 CSS。
3. 依既有 `pet_skill` 戰鬥事件為六種三星技能製作專屬動畫。
4. 將寵物召喚事件日期由硬編碼改為設定／後台管理，或至少更新過期行為。
5. 再逐步把 `app.py` 中的跨系統規則抽成服務函式；不要在 UI 修 Bug 時一起做。

---

## 5. 重要設計決策與原因

### 5.1 本機 SQLite、公開 Postgres 共用資料介面

- `DatabaseConnection` 統一兩者的 placeholder 與 context manager。
- 原因：初期可零設定本機測試，公開版用 Neon 讓多學生共享資料。
- 後果：本機排行與公開學生資料不相通；調整真實學生資料必須連公開 Neon，不能拿 `game.db` 推論。

### 5.2 存檔以 `profile_json` 為主

- 原因：遊戲功能快速增加，JSON 容易向後相容且不用每次改資料表。
- 所有新增 profile 欄位必須在 `create_new_profile()` 和正規化流程補預設值，並驗證舊玩家登入。
- 長期代價是整包寫入、查詢困難與競態風險，見技術債。

### 5.3 收藏是「曾獲得」，不是「目前持有」

- `collection_catalog` 是永久紀錄。
- 裝備分解、熔煉或卸下不應移除收藏。
- 任何新的掉落、商店、熔煉、信箱獎勵路徑都必須同步收藏紀錄。

### 5.4 兩套配裝保留 legacy `equipment`

- `profile["equipment"]` 仍是許多系統的既有介面。
- `equipment_loadouts` 保存兩套；`active_equipment_loadout` 指定目前套裝。
- 切換、穿戴、卸下、刪除裝備後，必須呼叫既有同步函式，不能只改其中一份。

### 5.5 戰鬥計算與動畫分離

- `game_logic/combat.py` 產生事件；`game_ui/battle.py` 顯示事件與動畫。
- 平衡修改應先改設定／純計算，再改演出，不要把傷害計算藏進 JavaScript 動畫。
- 第一次與重複 BOSS 挑戰都需觀看戰鬥；不再提供略過動畫以刷紀錄的設計。

### 5.6 普通 BOSS 控制章節進度

- 普通 BOSS 首通解鎖下一章；菁英 BOSS 是額外收集／排行／稱號挑戰。
- 曾討論「九部位三星才解鎖菁英 BOSS」，後來放棄，因為收集完成後太容易碾壓；現行是普通 BOSS 通關即可挑戰菁英，失敗再回去收集。

### 5.7 單元採連擊而非固定倒數

- 早期 60 秒制度對學生過難，已改為最多 20 題與連擊門檻。
- 不要自行恢復 60 秒或 3 分鐘限時；時間只記錄作答效率／排行資訊。

### 5.8 裝備星級與章節強度分離

- 單元最多掉三星，但不同章節同星級固定值不同。
- 四星由章節成就、九部位收藏與菁英 BOSS 固定給予；固定值為該章三星再加成。
- 不使用「裝備等級限制」，避免玩家先拿到卻不能穿戴；名稱以章節前綴區分。

### 5.9 寵物重複不直接升星

- 重複召喚轉為同屬性元神；玩家再主動進階。
- 原因：三星後再抽到同寵仍有資源價值，也讓升星成本可控。

### 5.10 四科版本目前放棄／暫停

- 曾設想國英數自共用冒險章節、四科全滿星才給四星，但內容製作量與作業負擔過大，且試作未進入目前正式程式。
- 目前首頁仍為「數學冒險」，關卡為五章數學。
- 未來若重啟多科，應當作獨立大型架構任務，先設計資料模型與舊資料遷移，不可直接把舊試作覆蓋回來。

---

## 6. 不可隨意修改的介面與相依性

### 6.1 穩定識別碼

以下值已寫入公開玩家存檔、排行或收藏，不可只改名稱：

- 章節 ID：`"1"`～`"5"`
- 單元 ID：如 `"1-1"`、`"5-3"`
- 裝備 slot key：`helmet`、`armor`、`gloves`、`weapon`、`boots`、`necklace`、`ring`、`belt`、`shield`
- 物品 `uid`
- 寵物 ID、元素 key
- `profile_json` 內所有 reward／win／claimed flag
- 各排行表名稱與 `BOSS_CONFIGS` key（如 `3_elite`）

要改顯示文字時，保留 key，只改 label／name。

### 6.2 Profile 正規化與兩套配裝

- 新欄位要同時處理新玩家與舊玩家。
- `profile["equipment"]`、`equipment_loadouts[active]["equipment"]` 必須同步。
- 分解／刪除物品必須清除兩套配裝中的失效 uid。
- `equipped_item_uids()` 會把兩套都視為使用中；不要只檢查目前套裝。

### 6.3 裝備與收藏

- 所有獲得物品的路徑都需更新 `collection_catalog`。
- 四星 `achievement`、`unit`、`chapter`、`slot` 共同影響圖鑑與補發判定。
- `fixed_value_for()` 及既有遷移函式會校正全體玩家裝備；改公式前先評估是否要遷移既有存檔。

### 6.4 BOSS 與排行

- 調 BOSS 新數值不應覆寫歷史最佳時間。
- 新章節若沿用現行做法，必須同步增加普通／菁英排行表、設定、任務、profile flags、獎勵與 UI；目前不是資料驅動的一鍵新增。

### 6.5 資產路徑

- 程式以相對專案根目錄的實體路徑載入圖片。移動／改名資產必須同步所有引用。
- 部署必須連同 `assets/`、`data_access/`、`game_logic/`、`game_ui/`、`game_data/`、`app.py`、`requirements.txt` 更新。
- 過去只上傳 `app.py` 曾造成公開版 ImportError 或缺圖片。

### 6.6 Streamlit 對話框與 session state

- widget 建立後，同一輪不能再直接修改同 key 的 `st.session_state`，否則會觸發 `StreamlitAPIException`。
- 角色能力目前必須由 `render_character_equipment_dialog()` 以原生 `st.dialog` 開啟。
- 關閉時會重設 `show_character_dialog`、頁面為 equipment、selected slot；若改關閉流程，要保留這些狀態語意。
- 不要重新加入 fixed-position 的假關閉鈕；它曾在方向切換／rerun 後消失並留下空白內容。

### 6.7 Secrets 與公開安全

- 不得讀出、貼出或提交 `DATABASE_URL`、`ADMIN_PIN`、PIN hash／salt。
- 公開版不能把管理 PIN 寫死在程式碼。
- 刪除學生是破壞性操作，UI 應明確指定學生代碼並確認。

---

## 7. 已知問題與技術債

### 7.1 高優先

1. **角色能力 UI 對 Streamlit DOM／CSS 高度敏感。** 直屏、橫屏、Cloud 工具列、方向切換都可能改變高度或 selector。近期關閉鈕改為原生 dialog，但尚缺正式多裝置回歸測試。
2. **本機、工作區、GitHub 可能版本分歧。** 每次先確認正在修改及部署的根目錄。
3. **缺少自動化測試。** 目前主要靠 `compileall`、本機啟動及人工瀏覽器／手機測試。
4. **寵物三星技能動畫尚未完成。** 屬性剋制與技能數值已接入純戰鬥，但專屬視覺演出仍待補齊。

### 7.2 中優先

1. `app.py` 仍過大，混合路由、交易、遷移、獎勵與頁面狀態。
2. `game_ui/character.py` 也已過大，角色裝備、背包、寵物、稱號及大量 responsive CSS 混在一起。
3. Profile 是整包 JSON 更新；同帳號多裝置同時操作有最後寫入覆蓋風險。
4. `data_version` 仍為 2；後續欄位多靠 `setdefault`，缺少清楚的逐版 migration pipeline。
5. 章節旗標、任務、排行表及四星獎勵大量按章節複製；增加第六章容易漏改。
6. 題目 log 的答案欄是數字；分數題以題目文字補充實際分子／分母，不是完整結構化答案模型。
7. `data_access/database.py` 頂端註解仍稱 schema/query 留在 `app.py`，但實際已拆到多個 data_access 檔，屬過時註解。

### 7.3 低優先／需產品決定

1. 心機狐活動日期硬編碼為 2026-09-15；到期後是否永久退池、是否換池，需使用者決定。
2. 寵物召喚結果目前沒有完整的聖光動畫流程。
3. 全站標楷體依賴客戶端字型；非 Windows／部分手機可能 fallback，不保證每台裝置外觀相同。
4. Streamlit 介面為達成遊戲式版面使用大量 CSS／JS；升級 Streamlit 前需完整回歸。

---

## 8. 各系統工作邊界

未來建議按下列系統建立獨立 Codex 任務。任務只先讀對應檔案；跨邊界時先回報影響。

### 8.1 戰鬥系統

**負責**：BOSS 數值與技能設定、角色／BOSS 傷害、暴擊、防禦、護盾、事件序列、戰鬥動畫與排行結算。

**優先讀取**：

- `game_data/config.py`
- `game_logic/combat.py`
- `game_logic/equipment.py`（角色能力來源）
- `game_ui/battle.py`
- `app.py` 中 `go_to_boss`、`finish_battle`、排行存檔與 BOSS route

**不要順便改**：商店、背包版面、帳號 schema。若新增寵物技能，需與寵物系統共同定義事件格式。

### 8.2 題目／關卡／進度系統

**負責**：題目產生、答案驗證、連擊星級、單元解鎖、章節解鎖、EXP、擊殺券、作答紀錄。

**優先讀取**：

- `game_data/config.py`
- `game_logic/questions.py`
- `game_logic/progression.py`
- `game_ui/quiz.py`
- `game_ui/stages.py`
- `data_access/progress.py`
- `app.py` 中 quiz／reward／unlock 編排

### 8.3 裝備／角色能力系統

**負責**：固定值、詞條、角色能力、兩套配裝、穿戴／卸下、比較、未使用裝備、多項分解、角色能力對話框。

**優先讀取**：

- `game_logic/equipment.py`
- `game_logic/profile.py`
- `game_logic/loot.py`
- `game_ui/character.py`
- `game_ui/profile.py`
- `game_data/config.py`

**跨模組警告**：分解會碰 `economy.py`；跟隨寵物會碰 `pets.py`；任何 uid 操作須照顧兩套配裝。

### 8.4 背包／物品／圖鑑系統

**負責**：消耗道具格、物品數量顯示、永久收藏目錄、圖鑑篩選與前往挑戰。

**優先讀取**：

- `game_ui/inventory.py`
- `game_logic/loot.py`
- `game_logic/profile.py`
- `app.py` 中 collection sync／mail reward 套用

**邊界**：未使用裝備和稱號現已在角色能力對話框，不應再次塞回背包。

### 8.5 商店／貨幣／熔煉系統

**負責**：金幣、商店產生／刷新／購買、裝備分解、熔煉、特殊熔煉石與結果視窗。

**優先讀取**：

- `game_logic/economy.py`
- `game_ui/economy.py`
- `game_logic/equipment.py`
- `game_logic/profile.py`
- `app.py` 中 economy wrapper 與 forge dialog

### 8.6 寵物／召喚／培養系統

**負責**：六寵物資料、圖片、擁有／跟隨／排序／改名、召喚、元神、進階、培養、等級加成、未來屬性剋制與技能。

**優先讀取**：

- `assets/pets/pets.json`
- `game_logic/pets.py`
- `game_ui/character.py` 的 pet 區段
- `game_ui/pet_summon.py`
- `game_logic/equipment.py` 的寵物能力加成
- `app.py` 的寵物補發信箱與掉落編排

**目前邊界**：純戰鬥尚未吃屬性與主動技能；若要實作，需再讀戰鬥模組並先說明跨系統影響。

### 8.7 任務／獎勵／信箱系統

**負責**：每日、永久、特殊任務，補發、一次性獎勵、信箱領取與讀取。

**優先讀取**：

- `game_logic/progression.py`
- `data_access/communications.py`
- `app.py` 中 task／mail／retroactive reward 函式
- 對應 UI route

**警告**：獎勵 JSON 必須由 `claim_mail_reward()` 實際套用；新增資源時需同時擴充領取邏輯與背包顯示。

### 8.8 帳號／存檔／後台系統

**負責**：登入、PIN、學生代碼、profile 載入／儲存／遷移、SQLite／Postgres、老師管理。

**優先讀取**：

- `game_logic/authentication.py`
- `game_logic/profile.py`
- `data_access/*`
- `game_ui/login.py`
- `game_ui/admin.py`
- `app.py` 中登入、`get_profile`、`save_profile`、老師 profile

### 8.9 UI／Responsive 系統

**負責**：全站字體、首頁導航、獨立頁／dialog、直屏／橫屏、置頂／關閉、Streamlit CSS／JS 相容性。

**優先讀取**：

- 任務直接涉及的 `game_ui/*.py`
- `game_ui/common.py`
- `app.py` 路由與 session state 預設

**工作原則**：先做最小頁面重現再修 selector；不可為修一個手機間距而改全站泛用 selector。須至少測直屏、橫屏、重新整理、dialog 重開。

### 8.10 媒體／動畫資產系統

**負責**：勇者、BOSS、登入、寵物及技能圖，檔案格式／大小與引用。

**優先讀取**：`assets/`、引用該圖的 UI 模組。生成新資產後要確認檔案已部署，並避免只修改本機路徑。

---

## 9. 給未來 Codex 任務的工作規則

1. 開始前先完整閱讀本 `AGENTS.md`。
2. 先確認實際專案根目錄、目標版本（本機／GitHub／公開 Cloud）及資料庫（SQLite／Neon）。
3. 優先只用 `rg` 找入口與引用，只讀與任務直接相關的檔案；不要為理解單一功能先掃整個 `app.py` 或全部資產。
4. 先讀純邏輯，再讀 UI，再讀 `app.py` 的編排入口；依賴方向不要倒置。
5. 不任意修改其他模組，不把局部修正擴大成全專案重構。
6. 若修改會影響存檔 key、裝備 uid、章節／單元 ID、BOSS 排行、信箱獎勵或資產路徑，先明確列出影響範圍與遷移方式。
7. 優先沿用既有資料結構與公開函式；需要改介面時，同步搜尋所有 import／call site。
8. 任何新增 profile 欄位都要處理：新 profile 預設、舊 profile 正規化、老師測試角色、公開玩家補發／遷移（若需要）。
9. 任何新增物品／貨幣都要處理：取得、存檔、信箱領取、背包顯示、消耗、防重複與老師檢視。
10. 任何新增裝備來源都要處理永久收藏；分解不移除收藏。
11. 任何裝備操作都要照顧兩套配裝與 legacy `equipment` 同步。
12. UI 修正不要依賴過度寬泛的 DOM selector；優先使用 Streamlit 原生 dialog、callback 與有 key 的 container。
13. 修改後至少執行 `compileall`；邏輯修改應增加可重複的小型測試。UI 修改依風險測直屏、橫屏、方向切換、重新整理和公開 Cloud。
14. 不輸出 Secrets、資料庫連線字串、PIN hash／salt 或學生敏感資料。
15. 不刪除／覆寫 `game.db` 或公開資料；資料遷移先備份並可重跑。
16. 不因對話曾提過就假定功能存在；以程式碼與實測為準。
17. 發現重要架構決策、跨模組依賴、存檔遷移或長期技術債時，同步更新本文件。
18. 任務交付時說明：改了哪些檔案、影響哪個系統、做了哪些驗證、公開部署還需上傳哪些檔案。

---

## 10. 目前專案狀態摘要（供新任務快速閱讀）

### 現在做到哪裡

- 目前正式程式是五章數學冒險，主要遊戲循環、帳號、單元、裝備、商店、熔煉、BOSS、任務、排行榜、公告／信箱／回饋均已有可運作實作。
- 程式已由單一 `app.py` 部分拆成 `data_access/`、`game_logic/`、`game_ui/`、`game_data/`，但入口仍負責大量跨系統編排。
- 寵物系統已具六寵物、召喚、重複轉元神、進階、培養、跟隨與等級能力加成。

### 已完成／可沿用

- SQLite／Neon 共用資料層。
- 五章題目與進度、普通／菁英 BOSS 解鎖。
- 兩套配裝、永久圖鑑、商店／熔煉。
- 任務、排行、信箱、公告、回饋與老師後台。
- 寵物基本養成與召喚資源循環。

### 正在開發

- 角色能力 `st.dialog` 的手機／方向切換穩定性；最新程式已改用原生關閉鈕。
- 寵物戰鬥擴充：屬性相剋與主動技能尚未進入戰鬥核心。

### 重要待辦

1. 公開版手機回歸角色能力 dialog。
2. 補齊「大橘為重」等寵物技能的專屬動畫。
3. 處理 2026-09-15 心機狐退池之後的產品規則。
4. 持續為戰鬥、裝備、profile 遷移與寵物補自動化測試。

### 主要已知問題

- Streamlit responsive CSS 脆弱，Cloud 與本機可能呈現不同。
- 本機與公開資料分離，且工作區／GitHub 容易版本分歧。
- `app.py`、`game_ui/character.py` 偏大。
- Profile 整包 JSON 更新有競態及長期遷移負擔。
- 寵物屬性剋制已套用於戰鬥；三星技能仍缺專屬視覺動畫。

### 開新系統任務時的第一句建議

> 請先閱讀專案根目錄 `AGENTS.md`，本次只處理「X 系統」。先列出需要讀取的相關檔案與跨系統影響，再開始修改；不要掃描或重構無關模組。
